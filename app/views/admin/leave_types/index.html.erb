<%- model_class = LeaveType -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:title) %></th>
      <th><%= model_class.human_attribute_name(:paid) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @leave_types.each do |leave_type| %>
      <tr>
        <td><%= leave_type.title %></td>
        <td><%= leave_type.paid %></td>
        <td>
      <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_admin_leave_type_path(leave_type), :class => 'btn btn-warning' %>
       <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      admin_leave_types_path(leave_type),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_admin_leave_type_path,
            :class => 'btn btn-primary' %>
